<!--pages/home/home.wxml-->
  <!--顶部-->
  <view class="bgi">
    <header></header>
    <view class="hotSearch">
      <text class="title">热搜：</text>
      <text class="content">面膜补水</text>
      <text class="content">进口零食</text>
      <text class="content">夏季衣服</text>
      <text class="content">果汁饮料</text>
    </view>
  </view>
<view class="box">
    <!--轮播-->
  <view class="swiper_wrapper wrapper bg-white">
    <swiper indicator-dots="true" autoplay="{{autoplay}}" interval="3000" duration="500" indicator-color="{{indicatorColor}}" indicator-active-color="{{indicatorActive}}" class="goodsSwiper wrapper_box-shadow">
      <block wx:for="{{goodSwiperImg}}" wx:key="id">
        <swiper-item>
            <image src="{{item.pic}}" bindtap="swiperToDetail" data-url="{{item.url}}" class="goodsSwiper_img" binderror="errImg" data-errimg="goodSwiperImg[{{index}}].pic" mode="aspectFill" lazy-load='true'></image>
        </swiper-item>
      </block>
    </swiper>
  </view>
  <!--菜單-->
  <view class='menu-coutainer'>
      <view class="li">
        <image class="img" mode="aspectFit"></image>
        <view class="text">美妆个护</view>
        
      </view>
      <view class="li">
        <image class="img" mode="aspectFit"></image>
        <view class="text">美妆个护</view>
      </view>
      <view class="li">
        <image class="img" mode="aspectFit"></image>
        <view class="text">美妆个护</view>
      </view>
      <view class="li">
        <image class="img" mode="aspectFit"></image>
        <view class="text">美妆个护</view>
      </view>
      <view class="li">
        <image class="img" mode="aspectFit"></image>
        <view class="text">美妆个护</view>
      </view>
      <view class="li">
        <image class="img" mode="aspectFit"></image>
        <view class="text">美妆个护</view>
      </view>
      <view class="li">
        <image class="img" mode="aspectFit"></image>
        <view class="text">美妆个护</view>
      </view>
      <view class="li">
        <image class="img" mode="aspectFit"></image>
        <view class="text">美妆个护</view>
      </view>
  </view>
  <!-- 人气商品 -->
  <view class="h2">- 人气商品 -</view>
  <view class="wrapper">
    <block  wx:for="{{goodsArea}}" wx:key="id">
      <goods-area area-data="{{item}}" binding="$" ref="goods-area"></goods-area>
    </block>
  </view>
</view>
  <!-- <view wx:if="{{menuList.length>0}}" class='bg-white menu-coutainer'>
    <view bindtap='bindToPage' wx:for="{{menuList}}" data-id="{{item.id}}" wx:key="id" class="menu-item {{index>0?'menu-item_margin':''}}">
      <image src='{{item.pic}}' class='menu-img' mode='aspectFill'></image>
    </view>
  </view> -->
  <!--单品推荐-->
  <!-- <view class="wrapper bg-white">
    <block wx:for="{{goodsRecommend}}" wx:key="index">
      <image src="{{item.pic}}" bindtap="goodToDetail" data-id="{{item.ag_value}}" data-type="{{item.ag_type}}" class="goodsRecommend_img wrapper_box-shadow" mode="widthFix" lazy-load="true" binderror="errImg" data-errimg="goodsRecommend[{{index}}].pic"></image>
    </block>
  </view> -->
  <!--产品分类-->
  <!-- <view class="wrapper bg-white">
    <block  wx:for="{{goodsArea}}" wx:key="id">
      <goods-area area-data="{{item}}" binding="$" ref="goods-area"></goods-area>
    </block>
  </view> -->